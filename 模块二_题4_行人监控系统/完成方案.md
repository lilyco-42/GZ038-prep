# 子任务2-4 行人监控系统开发 —— 完成方案

## 一、任务理解
自行创建 Android 项目「行人监控」，通过云服务系统采集传感器、控制执行器，实现行人闯红灯监控。界面三态：绿灯放行 / 红灯禁行 / 行人闯红灯。

## 二、硬件与云设备标识
| 类型 | 设备 | 云 ApiTag | 说明 |
|---|---|---|---|
| 传感器 | 行程开关(轮式) | `m_travelSwitch_singleWheel` | 默认闭合=绿灯；摇臂断开=红灯 |
| 传感器 | 红外对射(激光对射) | `m_laser` | 1=报警(有人越线) |
| 执行器 | 多层指示灯-红 | `m_multi_red` | 红灯亮 |
| 执行器 | 多层指示灯-绿 | `m_multi_green` | 绿灯亮 |
| 执行器 | 报警灯(转动指示灯) | `m_rotating_lamp` | 闯红灯时报警 |

> 标识以云服务系统实际录入为准，代码顶部常量一处可改。

## 三、业务逻辑状态机
```
行程开关闭合(绿灯)
  └─ 显示"绿灯放行"；仅 m_multi_green=1；红外对射报警也不变化
行程开关断开(红灯)
  ├─ 红外不报警 → 显示"红灯禁行"；仅 m_multi_red=1；报警灯=0
  └─ 红外报警   → 显示"行人闯红灯"；m_multi_red=1；报警灯=1
退出 onDestroy() → m_multi_red/green/alarm 全部下发 0，熄灭
```

## 四、工程结构（Gradle + Java）
```
PedestrianMonitor/
├─ build.gradle / settings.gradle / gradle.properties / gradle/wrapper
└─ app/
   ├─ build.gradle / proguard-rules.pro
   └─ src/main/
      ├─ AndroidManifest.xml        (INTERNET 权限, 明文流量)
      ├─ java/com/newland/pedestrian/
      │   ├─ CloudClient.java        (REST: login/Devices/Datas/Cmds)
      │   └─ MainActivity.java      (轮询线程 + 状态机 + UI)
      └─ res/ (layout/activity_main.xml, values, drawable)
```

云调用与赛项 Python `nle_cloud.py` 完全一致：`POST /users/login` 取 `AccessToken` → `GET /Devices/Datas?devIds=` 读传感器 → `POST /Devices/{id}/Cmds` 下发执行器。

## 五、运行与发布
1. Android Studio 打开 `PedestrianMonitor`，按工位修改 `CLOUD_URL/CLOUD_USER/CLOUD_PWD`；
2. 连接移动互联终端，`Run` 安装，App 名「行人监控」；
3. 摇动摇臂：闭合→绿灯放行图，断开→红灯；断开时遮挡红外对射→闯红灯图且报警灯亮；
4. 退出 App 后确认三色灯全部熄灭。

## 六、交付物
- `源码工程/PedestrianMonitor/`（完整 Gradle 工程，可导出 `行人监控系统源码.rar`）
- `完成方案.md`
- `提交资料/`：工程副本

---

## 七、构建命令（离线）

本机无网络，依赖走 Gradle 本地缓存（gradle-4.6、AGP 3.2.0、support-v7 28、android-28、build-tools 28.0.3）。该工程无 `gradlew.bat`，直接调用缓存的 gradle：

```powershell
$env:JAVA_HOME="C:\Program Files\Java\jdk1.8.0_202"
$env:ANDROID_HOME="C:\Users\Newland\AppData\Local\Android\Sdk"
cd "源码工程\PedestrianMonitor"   # 含 settings.gradle / build.gradle 那层
& "C:\Users\Newland\.gradle\wrapper\dists\gradle-4.6-all\bcst21l2brirad8k2ben1letg\gradle-4.6\bin\gradle.bat" --offline assembleDebug
```

> 工程路径含中文，`gradle.properties` 末尾已追加 `android.overridePathCheck=true`。
> 产物：`app\build\outputs\apk\debug\app-debug.apk`。本次构建 **一次通过，无编译错误**。

## 八、运行说明

1. **安装**：把 `二进制\行人监控系统.apk` 拷到安卓终端安装，桌面图标名「行人监控」。
2. **界面**：顶部场景色块（文字：绿灯放行 / 红灯禁行 / 行人闯红灯），中部灯色提示，底部报警灯图标。
3. **操作与业务**：App 每 0.8s 轮询一次云服务。摇动摇臂（行程开关）：闭合→绿灯放行图、仅绿灯亮；断开→红灯；断开且遮挡红外对射→“行人闯红灯”图且报警灯亮。退出 App 时红/绿/报警灯全部下发 0 熄灭。
4. **现场需改参数位置**（`MainActivity.java` 顶部常量）：
   - 云服务地址 `CLOUD_URL`（默认 `http://192.168.0.138`）、账号 `CLOUD_USER`、密码 `CLOUD_PWD`；
   - 传感器/执行器 ApiTag：行程开关 `TAG_TRAVEL`、红外对射 `TAG_LASER`、多层红/绿 `TAG_M_RED/TAG_M_GRN`、报警灯 `TAG_ALARM`，按云服务实际录入改。

## 九、验证结果

- 用 `aapt.exe dump badging` 校验：
  - package：`com.newland.pedestrian`，versionName 1.0，compileSdkVersion 28；
  - application-label：`行人监控`；
  - launchable-activity：`com.newland.pedestrian.MainActivity`。
  结论：**包合法可安装、有启动 Activity**。
- 本机无真机/模拟器，未做真机运行；运行可行性以 aapt 合法包为准。
- **无云/无硬件容错**：轮询循环 `step()` 整体包 try-catch，读不到传感器返回 null 时按“无信号”降级显示、不下发异常值；登录失败自动重连，App 不崩溃。
- 证据文件：`提交资料\运行验证_apk.txt`（完整 aapt dump badging 输出）。
- 归集产物：`二进制\行人监控系统.apk`（约 1.57 MB）。
